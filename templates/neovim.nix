{ ... }:
{
  axiom.neovim = {
    # Set true if you want to configure neovim manually using a config file
    configureByConfigFile = false;
    # Set true once if you want to nuke ~/.config/nvim and re-seed on next switch
    resetConfigFileOnNextSwitch = false;

  };
}
